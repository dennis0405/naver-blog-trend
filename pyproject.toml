[project]
name = "naver-tech-blog-agent"
version = "0.1.0"
description = "Daily Naver tech blog signal collection and local style mining"
requires-python = ">=3.12"
dependencies = []

[tool.unittest]
start-dir = "tests"

